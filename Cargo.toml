[package]
name = "mermaid-render"
version = "0.1.2"
edition = "2024"
description = "Pure Rust Mermaid diagram parser and SVG renderer — zero JS, zero Node"
license = "MIT"
repository = "https://github.com/alitrack/mermaid-render"
keywords = ["mermaid", "diagram", "svg", "renderer"]

[dependencies]
