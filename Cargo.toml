[package]
name = "mermaid-rs"
version = "0.1.0"
edition = "2024"
description = "Pure Rust Mermaid diagram parser and SVG renderer"
license = "MIT"
repository = "https://github.com/alitrack/mermaid-rs"
keywords = ["mermaid", "diagram", "svg", "renderer"]

[dependencies]
